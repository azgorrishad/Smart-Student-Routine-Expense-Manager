package service;

// Interface for generating reports (OOP concept - Interface / Abstraction)
// Any class that generates a report must implement this
public interface ReportGenerator {
    String generateReport();
}
